import React from 'react';
import Header from '../Header/Header';
import MoviesList from '../MoviesList/MoviesList';
//home const
//test funct in test module
const Home = () => {
    return (
        <div>
            <MoviesList/>
        </div>
    );
};

export default Home;
